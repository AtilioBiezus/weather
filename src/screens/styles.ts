import { StyleSheet } from "react-native";

const Styles = StyleSheet.create({
    todayTemperature: {
        fontWeight: "bold",
        fontSize: 92,
        marginBottom: 10
    },
    cityInfo: {
        marginStart: 10,
        marginBottom: 20
    },
    nextDays: {
        marginLeft: 10,
        marginBottom: 20
    }
})

export default Styles